# Flipbook API

This is the companion package for the [Flipbook plugin](https://github.com/flipbook-labs/flipbook). It's used inside
Stories and Storybooks to access helper functions and Luau types that Flipbook uses.
