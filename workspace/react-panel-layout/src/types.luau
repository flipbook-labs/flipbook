local React = require("@pkg/React")

-- Sizes stored as grow weights (equivalent to percentages within the group).
-- Keys are panel IDs, values are grow ratios.
export type LayoutSizes = { [string]: number }

return {}
