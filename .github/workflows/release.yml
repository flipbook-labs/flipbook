name: Release

on:
  pull_request:
  release:
    types: [published]
  push:
    branches:
      - main

jobs:
  publish-github-release:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0

      - name: Get model file name
        run: |
          name=$(jq -r .name default.project.json)
          echo "MODEL_FILE=$name.rbxm" >> $GITHUB_ENV

      - name: Install dependencies
        run: lute scripts/install

      - name: Build
        run: lute scripts/build --channel prod --output ${{ env.MODEL_FILE }}

      - uses: softprops/action-gh-release@v1
        if: ${{ github.event.release }}
        with:
          files: ${{ env.MODEL_FILE }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-plugin:
    runs-on: macos-latest
    timeout-minutes: 10
    env:
      LOG_LEVEL: debug
    steps:
      - uses: actions/checkout@v4

      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0

      - name: Install dependencies
        run: lute scripts/install

      - name: Publish nightly build to Creator Store
        if: github.ref == 'refs/heads/main'
        run: lute scripts/publish-plugin --channel dev --apiKey ${{ secrets.ROBLOX_API_KEY }}

      - name: Publish release to Creator Store
        if: github.event.release
        run: lute scripts/publish-plugin --channel prod --apiKey ${{ secrets.ROBLOX_API_KEY }}
